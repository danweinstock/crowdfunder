<!DOCTYPE html>
<html>
<head>
  <title>Crowdfunder</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
	  <!-- Latest compiled and minified CSS -->
</head>

<body>

	<nav class="navbar navbar-default">	

    <% if current_user %>
			<%= link_to("Logout", logout_url, class: "btn btn-default navbar-btn") %>
			<p> Logged in as <%= current_user.first_name %> <%=current_user.last_name%> </p>
	   <% else %>
			<%= link_to("Signup", new_user_path, class: "btn btn-default navbar-btn") %>
			<%= link_to("Login", :login, class: "btn btn-default navbar-btn") %>
		<% end %>

	</nav>	
	
	<div class="container">
		<ul class="nav nav-tabs">
			<li role="presentation" class="active"><%= link_to("Projects", "/projects") %></li>

	    <% if current_user %>
		  	<li role="presentation"><%= link_to("My Profile", user_path(current_user.id)) %></li>
		  	<li role="presentation"><%= link_to("New Project", new_project_path()) %></li>
			<% end %>

		</ul>
		<%= yield %>		
	</div>

<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
</body>
</html>
