<h1>All Projects</h1>

<% @projects.each do |project| %>
	<div class="project">
		<h2> <%= project.title %> </h2>
		<p> Goal: <%= number_to_currency(project.goal) %> </p>
		<p> Start Date: <%= project.start_date.strftime("%b %d, %Y") %> </p>
		<p> End Date: <%= project.end_date.strftime("%b %d, %Y") %> </p>
		<p> Description: <%= project.description %> </p>
	</div>
<% end %>