<h1>All Projects</h1>

<% @projects.each do |project| %>
	<div class="project">
		<h2> <%= project.title %> </h2>
		<p> <%= number_to_currency(project.goal) %> </p>
		<p> <%= project.start_date.strftime("%b %d, %Y") %> </p>
		<p> <%= project.end_date.strftime("%b %d, %Y") %> </p>
		<p> <%= project.description %> </p>
	</div>
<% end %>